import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter_barcode_scanner/flutter_barcode_scanner.dart';

import '../ajoutercartes/mon_acceuil.dart';

class codebarre extends StatefulWidget {
  const codebarre({Key? key}) : super(key: key);

  @override
  State<codebarre> createState() => _codebarreState();
}
class _codebarreState extends State<codebarre> {
  @override
  Widget build(BuildContext context) {
    final height = MediaQuery.of(context).size.height;
        return Column(

        );
  }
}
